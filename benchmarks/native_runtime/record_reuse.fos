type Pair = { left: Int, right: Int }
func main() -> Int {
    let pair = Pair { left: 0, right: 1 }
    let index = 0
    let total = 0
    while index < 100000 {
        pair.left = index
        total = total + pair.left + pair.right
        index = index + 1
    }
    total
}
