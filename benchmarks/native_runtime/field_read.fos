type Pair = { left: Int, right: Int }
func main() -> Int {
    let pair = Pair { left: 7, right: 3 }
    let index = 0
    let total = 0
    while index < 100000 {
        total = total + pair.left + pair.right
        index = index + 1
    }
    total
}
