type Pair = { left: Int, right: Int }
func main() -> Int {
    let index = 0
    let total = 0
    while index < 100000 {
        let pair = Pair { left: index, right: 1 }
        total = total + pair.left + pair.right
        index = index + 1
    }
    total
}
