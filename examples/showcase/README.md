# Foster capability showcase

These focused programs keep one or two ideas prominent while remaining complete, runnable Foster
source. See the [examples catalog](../README.md) for the capability demonstrated by each file.

Run an example from the repository root:

```console
cargo run --bin foster -- run examples/showcase/closures.fos
```
