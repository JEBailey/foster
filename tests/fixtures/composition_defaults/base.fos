pub type Base = {
    pub value: Int
    pub func fighter(self) -> Int
}
func helper(value: Int) -> Int { value + 1 }
impl Base {
    pub func fighter(self) -> Int { helper(self.value) }
    pub func doubled(self) -> Int { self.fighter() * 2 }
}
